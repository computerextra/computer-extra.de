---
import Main from "./Main.astro";

import AnimatedText from "@/components/ui/AnimatedText";

interface Props {
  title?: string;
  frontmatter?: { title?: string };
}

const { title, frontmatter } = Astro.props;

const isWeihnachten = false;
---

<Main title={title} frontmatter={frontmatter}>
  <div class="overflow-x-hidden">
    <video
      src="/videos/bg.mp4"
      autoplay
      id="bgvid"
      muted
      loop
      playsinline
      class="absolute top-0 right-0 left-0 z-10 min-h-screen min-w-full object-cover grayscale invert"
    ></video>
    <div class="relative z-20 min-h-screen w-full bg-blue-600/50">
      <div
        class="mx-2 flex min-h-[75vh] w-full flex-col justify-center ps-5 pe-5 lg:mx-0 lg:ps-[20%] lg:pe-0 lg:pt-[10vh]"
      >
        <h1
          class="max-w-fit rounded-xl px-10 py-0 pt-3 text-3xl font-bold text-slate-100 uppercase md:py-4 lg:text-5xl"
        >
          Computer Extra bietet&nbsp;
          <br class="2xl:hidden" />
          <AnimatedText client:only />
          <br />
          der Extraklasse.
        </h1>
        {
          isWeihnachten && (
            <h2 class="mt-3 w-full max-w-fit rounded-xl border-blue-900/70 bg-red-800/70 px-10 text-xl font-semibold text-slate-100 md:mt-12 md:py-4 lg:w-[50%] lg:text-2xl">
              Sehr geehrte Kund:innen, <br />
              unser Geschäft ist am 24.12.2025, 31.12.2025 sowie am 02.01.2026
              geschlossen. <br />
              Ab dem 05.01.2026 sind wir wieder wie gewohnt für Sie da.
            </h2>
          )
        }
        <h2
          class={isWeihnachten
            ? "text-xl lg:text-2xl font-semibold text-slate-100 mt-3 md:mt-8 w-full lg:w-[50%] max-w-fit rounded-xl px-10 md:py-4 bg-blue-900/70"
            : "text-xl lg:text-2xl font-semibold text-slate-100 mt-3 md:mt-20 w-full lg:w-[50%] max-w-fit rounded-xl px-10 md:py-4 bg-blue-900/70"}
        >
          Ob IT-Infrastruktur, Security oder Kommunikation, Computer Extra hat
          die passenden Produkte und Services verfügbar. Privat-, Office- und
          Gaming-PCs werden direkt auf die Bedürfnisse der Kunden zugeschnitten.
          Die exklusive Partnerschaft mit der Telekom für Business- und
          Privattarife runden das Gesamtpaket ab.
        </h2>

        <h2
          class="mt-3 w-full max-w-fit rounded-xl bg-blue-900/70 px-10 text-xl font-semibold text-slate-100 md:mt-20 md:py-4 lg:w-[50%] lg:text-2xl"
        >
          Ob IT-Infrastruktur, Security oder Kommunikation, Computer Extra hat
          die passenden Produkte und Services verfügbar. Privat-, Office- und
          Gaming-PCs werden direkt auf die Bedürfnisse der Kunden zugeschnitten.
          Die exklusive Partnerschaft mit der Telekom für Business- und
          Privattarife runden das Gesamtpaket ab.
        </h2>
        <div
          class="mt-3 w-full max-w-fit rounded-xl bg-blue-900/70 px-10 text-xl font-bold text-slate-100 md:mt-20 md:py-4 lg:w-[50%] lg:text-2xl"
        >
          <h3 class="uppercase">Öffnungszeiten</h3>
          <p>
            Montag - Freitag: 09:00 - 18:00 | Samstag - Sonntag: geschlossen
          </p>
        </div>
        <a
          href="/Termin"
          class="mt-3 w-full max-w-fit rounded-xl bg-blue-900/70 px-10 text-xl font-bold text-slate-100 transition-all duration-100 ease-in-out hover:scale-105 hover:bg-blue-900 hover:underline focus:bg-blue-900 focus:underline md:mt-20 md:py-4 lg:w-[50%] lg:text-2xl"
        >
          Jetzt einen Telekom Beratungstermin buchen
        </a>
      </div>
      <div class="overflow-hidden">
        <svg
          class="waves"
          xmlns="http://www.w3.org/2000/svg"
          xmlns:xlink="http://www.w3.org/1999/xlink"
          viewBox="0 24 150 28"
          preserveAspectRatio="none"
          shape-rendering="auto"
        >
          <defs>
            <path
              id="gentle-wave"
              d="M-160 44c30 0 58-18 88-18s 58 18 88 18 58-18 88-18 58 18 88 18 v44h-352z"
            ></path>
          </defs>
          <g class="parallax">
            <use
              xlink:href="#gentle-wave"
              x="48"
              y="0"
              fill="rgba(255,255,255,0.7"></use>
            <use
              xlink:href="#gentle-wave"
              x="48"
              y="3"
              fill="rgba(255,255,255,0.5)"></use>
            <use
              xlink:href="#gentle-wave"
              x="48"
              y="5"
              fill="rgba(255,255,255,0.3)"></use>
            <use xlink:href="#gentle-wave" x="48" y="7" fill="#fff"></use>
          </g>
        </svg>
      </div>
      <div class="w-full bg-white pt-5">
        <div class="mx-auto w-11/12 rounded-t-xl pt-10">
          <slot />
        </div>
      </div>
    </div>
  </div>
  <script is:inline>
    const video = document.querySelector("#bgvid");
    if (video == null) return;

    video.playbackRate = 0.5;
  </script>
</Main>
